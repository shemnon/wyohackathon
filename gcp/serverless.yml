service: wyohackathon-topicless-gcp 

provider:
  name: google
  runtime: nodejs
  project: wyohackathon-topicless
  credentials: C:\Users\danno\.gcloud\keyfile.json 

plugins:
  - serverless-google-cloudfunctions

package:
  exclude:
    - node_modules/**
    - .gitignore
    - .git/**

functions:
  Test1:
    handler: http
    events:
      - http: path
